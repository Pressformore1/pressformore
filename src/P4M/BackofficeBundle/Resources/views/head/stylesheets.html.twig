{% stylesheets 'css/common/*.css' filter='cssrewrite' %}
<link rel="stylesheet" href="{{ asset(asset_url) }}" />
{% endstylesheets %}{# empty Twig template #}


{% stylesheets 'bundles/p4mbackoffice/css/*.css' filter='cssrewrite' %}
<link rel="stylesheet" href="{{ asset(asset_url) }}" />
{% endstylesheets %}{# empty Twig template #}

{% stylesheets 'css/menu/*.css' filter='cssrewrite' %}
<link rel="stylesheet" href="{{ asset(asset_url) }}" />
{% endstylesheets %}{# empty Twig template #}
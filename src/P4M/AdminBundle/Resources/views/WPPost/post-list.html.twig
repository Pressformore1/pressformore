{# On étend notre layout #}
{% extends "P4MAdminBundle::admin.html.twig" %}

{% block pageTitle%}Category List{% endblock %}


{# Dans notre layout, il faut définir le block body #}

{%block adminActions%}
<a href="{{path('p4m_admin_createwpPost')}}" class="btn btn-info">CREATE BLOG POST</a>
{%endblock%}

{% block leftCol %}
{{parent()}}
        <table class="table table-striped table-sortable">
            <thead>
              <tr>
                <th>#</th>
                <th>Post Title</th>
                
                <th>Edit</th>
              </tr>
            </thead>
            <tbody>
            {% for wpPost in wpPosts %} 
                
            <tr>
                <td>{{wpPost.id}}</td>
                <td>{{wpPost.title}}</td>
                <td><a href="{{path('p4m_admin_wpPostedit',{'wpPostId':wpPost.id})}}">Edit</a></td>
                
              </tr>

            {% endfor %}


            </tbody>
          </table>

                        
{% endblock %}{# empty Twig template #}
